name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: Tests (Ruby ${{ matrix.ruby }})
    # ubuntu-22.04 still provides a prebuilt Ruby 2.6 (the support floor);
    # newer runner images do not.
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        ruby: ['2.6', '3.4', '4.0']
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
      # Runs the whole suite (test/*_test.rb) and fails if library line
      # coverage drops below the threshold. Uses only stdlib, so no install.
      - name: Run tests with coverage
        run: ruby test/coverage.rb

  lint:
    name: RuboCop (2.6 compatibility)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '4.0'
      - name: Install RuboCop
        run: gem install rubocop --no-document
      - name: Lint
        run: rubocop
